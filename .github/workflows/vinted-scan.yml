      - name: Sauvegarder
        if: always()
        run: |
          git config user.name "vinted-tarayici-bot"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          test -f annonces_vues.json || echo '[]' > annonces_vues.json
          cp annonces_vues.json /tmp/vues.json

          if [ -f alertes.csv ]; then
            cp alertes.csv /tmp/alertes.csv
          else
            : > /tmp/alertes.csv
          fi

          for f in base_apprentissage.json exemples_classes.txt historique_annonces.jsonl; do
            if [ -f "$f" ]; then
              cp "$f" "/tmp/$f"
            fi
          done

          git fetch origin main
          git reset --hard origin/main

          if [ ! -f annonces_vues.json ]; then
            if [ -f seen.json ]; then
              cp seen.json annonces_vues.json
            else
              echo '[]' > annonces_vues.json
            fi
          fi

          python fusion_donnees.py \
            /tmp/vues.json \
            /tmp/alertes.csv \
            annonces_vues.json \
            alertes.csv

          for f in base_apprentissage.json exemples_classes.txt historique_annonces.jsonl; do
            if [ -f "/tmp/$f" ]; then
              cp "/tmp/$f" "$f"
            fi
          done

          git add annonces_vues.json
          [ ! -f alertes.csv ] || git add alertes.csv
          [ ! -f base_apprentissage.json ] || git add base_apprentissage.json
          [ ! -f exemples_classes.txt ] || git add exemples_classes.txt
          [ ! -f historique_annonces.jsonl ] || git add historique_annonces.jsonl

          if ! git diff --cached --quiet; then
            git commit -m "Mise a jour apprentissage Vinted [skip ci]"
            git push origin HEAD:main
          fi
      python -m py_compile vinted_tarayici.py vinted_v70.py fusion_donnees.py
          python -m json.tool config.json > /dev/null
          python -m json.tool blacklist.json > /dev/null
          python -m json.tool filtres.json > /dev/null
          test -f exemples.txt

      - name: Notification test
        if: github.event_name == 'workflow_dispatch'
        env:
          NTFY_TOPIC: ${{ secrets.NTFY_TOPIC }}
        run: |
          test -n "$NTFY_TOPIC"
          curl -fsS \
            -H "Title: Vinted Tarayici" \
            -H "Priority: high" \
            -d "Vinted Tarayici demarre correctement." \
            "https://ntfy.sh/$NTFY_TOPIC"

      - name: Scanner Vinted
        env:
          NTFY_TOPIC: ${{ secrets.NTFY_TOPIC }}
          NTFY_SERVER: https://ntfy.sh
          HEADLESS: "1"
        run: python -u vinted_v70.py --once --headless
      - name: Sauvegarder
        if: always()
        run: |
          git config user.name "vinted-tarayici-bot"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          test -f annonces_vues.json || echo '[]' > annonces_vues.json
          cp annonces_vues.json /tmp/vues.json

          if [ -f alertes.csv ]; then
            cp alertes.csv /tmp/alertes.csv
          else
            : > /tmp/alertes.csv
          fi

          git fetch origin main
          git reset --hard origin/main

          if [ ! -f annonces_vues.json ]; then
            if [ -f seen.json ]; then cp seen.json annonces_vues.json; else echo '[]' > annonces_vues.json; fi
          fi

          python fusion_donnees.py \
            /tmp/vues.json \
            /tmp/alertes.csv \
            annonces_vues.json \
            alertes.csv

          git add annonces_vues.json
          [ ! -f alertes.csv ] || git add alertes.csv

          if ! git diff --cached --quiet; then
            git commit -m "Mise a jour du scanner [skip ci]"
            git push origin HEAD:main
          fi
